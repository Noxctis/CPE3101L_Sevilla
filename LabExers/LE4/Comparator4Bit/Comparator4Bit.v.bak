module Comparator4Bit (
    input  [3:0] A, // 4-bit input A
    input  [3:0] B, // 4-bit input B
    output [2:0] R  // Output: R[2] = G, R[1] = E, R[0] = L
);

    assign R[2] = (A > B);  // G: A > B
    assign R[1] = (A == B); // E: A == B
    assign R[0] = (A < B);  // L: A < B

endmodule
